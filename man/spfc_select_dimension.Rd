% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimension_selection.R
\name{spfc_select_dimension}
\alias{spfc_select_dimension}
\title{Select Structural Dimension for SPFC}
\usage{
spfc_select_dimension(
  X,
  y,
  d_grid = 1:3,
  cov_method = c("mec", "oas", "sre", "sde", "cse"),
  ytype = c("auto", "continuous", "categorical"),
  nslices = 5,
  poly_degree = 1,
  gamma = 0.1,
  rho = 0.5,
  centre_x = TRUE,
  scale_x = FALSE,
  classifier = c("auto", "glm"),
  eps = 1e-08,
  verbose = TRUE
)
}
\arguments{
\item{X}{Numeric predictor matrix.}

\item{y}{Response vector.}

\item{d_grid}{Integer vector of candidate structural dimensions.}

\item{cov_method}{Covariance estimator. One of \code{"mec"}, \code{"oas"}, \code{"sre"},
\code{"sde"}, or \code{"cse"}.}

\item{ytype}{Character. One of \code{"auto"}, \code{"continuous"}, or \code{"categorical"}.}

\item{nslices}{Number of slices for MEC with continuous responses.}

\item{poly_degree}{Polynomial degree for continuous response basis.}

\item{gamma}{Ridge constant for SRE.}

\item{rho}{Convex shrinkage intensity for CSE.}

\item{centre_x}{Logical. If \code{TRUE}, centre predictor columns.}

\item{scale_x}{Logical. If \code{TRUE}, scale predictor columns.}

\item{classifier}{Character. One of \code{"auto"} or \code{"glm"} for categorical
responses when information criteria are required. Dimension selection uses
an ordinary logistic companion model for categorical responses because the
current information criteria require log-likelihood and covariance estimates.}

\item{eps}{Small positive numerical floor.}

\item{verbose}{Logical. If \code{TRUE}, print progress.}
}
\value{
A list containing the criterion table and selected dimensions by
criterion.
}
\description{
Fits SPFC models across candidate dimensions and scores classical and
information-complexity-based criteria.
}
\examples{
set.seed(1)
X <- matrix(rnorm(180), nrow = 30, ncol = 6)
y <- X[, 1] - 0.7 * X[, 2] + rnorm(30, sd = 0.5)
dsel <- spfc_select_dimension(
  X, y, d_grid = 1:2, cov_method = 'oas',
  ytype = 'continuous', poly_degree = 2, verbose = FALSE
)
dsel$selected
}
