% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison_tools.R
\name{compare_covariance_methods}
\alias{compare_covariance_methods}
\title{Compare Covariance Estimators in SPFC}
\usage{
compare_covariance_methods(
  X,
  y,
  d,
  methods = c("mec", "oas", "sre", "sde", "cse"),
  ytype = c("auto", "continuous", "categorical"),
  nslices = 5,
  poly_degree = 1,
  gamma = 0.1,
  rho = 0.5,
  centre_x = TRUE,
  scale_x = FALSE,
  classifier = c("auto", "glm", "ridge_logistic"),
  eps = 1e-08,
  verbose = TRUE
)
}
\arguments{
\item{X}{Numeric predictor matrix.}

\item{y}{Response vector.}

\item{d}{Number of reduction directions.}

\item{methods}{Character vector of covariance estimators. Available options
are \code{"mec"}, \code{"oas"}, \code{"sre"}, \code{"sde"}, and \code{"cse"}.}

\item{ytype}{Character. One of \code{"auto"}, \code{"continuous"}, or \code{"categorical"}.}

\item{nslices}{Number of slices for continuous responses.}

\item{poly_degree}{Polynomial degree for continuous response basis.}

\item{gamma}{Ridge constant for SRE.}

\item{rho}{Convex shrinkage intensity for CSE.}

\item{centre_x}{Logical. If \code{TRUE}, centre predictor columns.}

\item{scale_x}{Logical. If \code{TRUE}, scale predictor columns.}

\item{classifier}{Character. One of \code{"auto"}, \code{"glm"}, or \code{"ridge_logistic"}.}

\item{eps}{Positive numerical floor for eigenvalues.}

\item{verbose}{Logical. If \code{TRUE}, print progress.}
}
\value{
A list containing a summary table and full fitted objects.
}
\description{
Fits shrinkage principal fitted components using several covariance
estimators and evaluates the reduced model fitted on the resulting SPFC
scores.
}
\examples{
set.seed(1)
X <- matrix(rnorm(180), nrow = 30, ncol = 6)
y <- X[, 1] - 0.7 * X[, 2] + rnorm(30, sd = 0.5)
cmp <- compare_covariance_methods(
  X, y, d = 1, methods = c('oas', 'sre'),
  ytype = 'continuous', poly_degree = 2, verbose = FALSE
)
cmp$summary
}
