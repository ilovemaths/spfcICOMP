% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_tools.R
\name{simulate_spfc_continuous}
\alias{simulate_spfc_continuous}
\title{Simulate Continuous High-Dimensional PFC Data}
\usage{
simulate_spfc_continuous(
  n = 100,
  p = 50,
  d = 1,
  s = 5,
  rho_x = 0.5,
  snr = 1,
  n_test = n,
  seed = NULL
)
}
\arguments{
\item{n}{Training sample size.}

\item{p}{Number of predictors.}

\item{d}{Structural dimension. The validated thesis DGP supports 1 or 2.}

\item{s}{Number of active variables.}

\item{rho_x}{AR(1) correlation parameter of the inverse-model residual
covariance.}

\item{snr}{Inverse-model signal-to-noise ratio in the sufficient coordinates.}

\item{n_test}{Independent test-sample size. Defaults to \code{n}.}

\item{seed}{Optional random seed.}
}
\value{
A list containing training and test data, true PFC quantities, sparse
reduction directions, active set, and residual covariance.
}
\description{
Generates data directly from a principal fitted components inverse model,
so the nominal structural dimension is identifiable by construction. For
\code{d = 1}, the inverse mean uses \code{f(Y) = Y}. For \code{d = 2}, it uses the two
population-orthonormal basis functions \code{Y} and \code{(Y^2 - 1)/sqrt(2)} with
\code{Y ~ N(0,1)}. The inverse-mean loading matrix is constructed so that the true
sufficient reduction subspace is the sparse matrix returned as \code{B_true},
even when the residual predictor covariance is correlated.
}
\details{
An independent test sample is generated from the same inverse model for
honest out-of-sample prediction assessment.
}
\examples{
sim <- simulate_spfc_continuous(
  n = 30, p = 8, d = 1, s = 3, rho_x = 0.3,
  snr = 2, n_test = 10, seed = 123
)
c(train_n = length(sim$y), test_n = length(sim$y_test))
}
