% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spfc_fit.R
\name{spfc_fit}
\alias{spfc_fit}
\title{Fit Shrinkage Principal Fitted Components}
\usage{
spfc_fit(
  X,
  y,
  d,
  fy = NULL,
  ytype = c("auto", "continuous", "categorical"),
  cov_method = c("mec", "oas", "sre", "sde", "cse"),
  nslices = 5,
  poly_degree = 1,
  gamma = 0.1,
  rho = 0.5,
  centre_x = TRUE,
  scale_x = FALSE,
  eps = 1e-08
)
}
\arguments{
\item{X}{Numeric predictor matrix with observations in rows and variables in columns.}

\item{y}{Response vector.}

\item{d}{Number of reduction directions.}

\item{fy}{Optional user-supplied response basis matrix.}

\item{ytype}{Character. One of \code{"auto"}, \code{"continuous"}, or \code{"categorical"}.}

\item{cov_method}{Covariance estimator. One of \code{"mec"}, \code{"oas"}, \code{"sre"},
\code{"sde"}, or \code{"cse"}.}

\item{nslices}{Number of response slices for continuous responses.}

\item{poly_degree}{Polynomial degree for continuous response basis construction.}

\item{gamma}{Ridge constant for SRE.}

\item{rho}{Convex shrinkage intensity for CSE.}

\item{centre_x}{Logical. If \code{TRUE}, centre predictor columns.}

\item{scale_x}{Logical. If \code{TRUE}, scale predictor columns.}

\item{eps}{Positive numerical floor for eigenvalues.}
}
\value{
A \code{spfc_fit} object containing reduced scores, directions, fitted
covariance objects, response basis, preprocessing metadata, and diagnostics.
}
\description{
Fits a shrinkage principal fitted components model by replacing the unstable
covariance object in the PFC reduction step with a selected shrinkage
covariance estimator.
}
\examples{
set.seed(1)
X <- matrix(rnorm(180), nrow = 30, ncol = 6)
y <- X[, 1] - 0.7 * X[, 2] + rnorm(30, sd = 0.5)
fit <- spfc_fit(
  X, y, d = 1, ytype = 'continuous',
  cov_method = 'oas', poly_degree = 2
)
coef(fit)
}
