% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation_tools.R
\name{evaluate_spfc_train_test}
\alias{evaluate_spfc_train_test}
\title{Evaluate SPFC with Train-test Validation}
\usage{
evaluate_spfc_train_test(
  X,
  y,
  train_idx,
  test_idx,
  d,
  ytype = c("auto", "continuous", "categorical"),
  cov_method = c("mec", "oas", "sre", "sde", "cse"),
  nslices = 5,
  poly_degree = 1,
  variable_method = c("adaptive_weighted_l1", "c1f_extension"),
  selection_rule = c("c1f", "quantile", "fixed"),
  threshold = NULL,
  quantile_cut = 0.75,
  centre_x = TRUE,
  scale_x = FALSE,
  ...
)
}
\arguments{
\item{X}{Predictor matrix.}

\item{y}{Response vector.}

\item{train_idx}{Training indices.}

\item{test_idx}{Test indices.}

\item{d}{Structural dimension.}

\item{ytype}{Response type.}

\item{cov_method}{Covariance estimator.}

\item{nslices}{Number of response slices.}

\item{poly_degree}{Polynomial degree.}

\item{variable_method}{Variable selection method.}

\item{selection_rule}{Character screening rule: \code{"c1f"}, \code{"quantile"}, or \code{"fixed"}.}

\item{threshold}{Optional finite numeric screening threshold. It is required for \code{selection_rule = "fixed"}; for \code{"quantile"}, \code{NULL} uses \code{quantile_cut}; it is ignored by the C1F-calibrated rule.}

\item{quantile_cut}{Variable selection threshold.}

\item{centre_x}{Logical.}

\item{scale_x}{Logical.}

\item{...}{Additional arguments passed to spfc_fit().}
}
\value{
A list containing fit, reduced model, predictions, and test metrics.
}
\description{
Evaluate SPFC with Train-test Validation
}
\examples{
\donttest{
set.seed(1)
X <- matrix(rnorm(180), nrow = 30, ncol = 6)
y <- X[, 1] - 0.7 * X[, 2] + rnorm(30, sd = 0.5)
split <- create_train_test_split(
  y, test_fraction = 0.25, seed = 123, stratify = FALSE
)
ans <- evaluate_spfc_train_test(
  X, y, train_idx = split$train, test_idx = split$test,
  d = 1, ytype = 'continuous', cov_method = 'oas',
  poly_degree = 2, selection_rule = 'quantile',
  quantile_cut = 0.75
)
ans$evaluation
}
}
