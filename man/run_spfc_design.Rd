% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_grid_tools.R
\name{run_spfc_design}
\alias{run_spfc_design}
\title{Run an SPFC Simulation Design}
\usage{
run_spfc_design(
  design,
  base_seed = 123,
  d_grid = 1:5,
  criteria = c("AIC", "BIC", "CAIC", "ICOMP_IFIM", "CICOMP"),
  selection_rule = "c1f",
  nslices = 5,
  poly_degree = 2,
  gamma = 0.1,
  rho = 0.5,
  save_every = NULL,
  save_path = "spfc_simulation_partial.rds",
  verbose = TRUE
)
}
\arguments{
\item{design}{Data frame returned by \code{create_spfc_design_grid()}.}

\item{base_seed}{Integer base seed.}

\item{d_grid}{Candidate structural dimensions evaluated for each simulation design row.}

\item{criteria}{Character vector of information criteria used for structural-dimension selection.}

\item{selection_rule}{Character screening rule passed to \code{run_spfc_design_row()}.}

\item{nslices}{Number of slices.}

\item{poly_degree}{Polynomial degree.}

\item{gamma}{Ridge constant for SRE.}

\item{rho}{Convex shrinkage intensity for CSE.}

\item{save_every}{Optional integer. If supplied, intermediate results are saved every \code{save_every} rows.}

\item{save_path}{Optional path for intermediate RDS output.}

\item{verbose}{Logical. If \code{TRUE}, print progress.}
}
\value{
A data frame of simulation results.
}
\description{
Runs an SPFC simulation design grid sequentially.
}
\examples{
\donttest{
design <- create_spfc_design_grid(
  response_type = 'continuous', n_values = 30, p_values = 8,
  d_values = 1, s_values = 3, rho_x_values = 0.3,
  snr_values = 2, cov_methods = 'oas',
  variable_methods = 'adaptive_weighted_l1', nrep = 1
)
ans <- run_spfc_design(
  design, base_seed = 123, d_grid = 1, criteria = 'AIC',
  selection_rule = 'c1f', poly_degree = 2, verbose = FALSE
)
ans[, c('cov_method', 'criterion', 'fitted_d', 'rmse')]
}
}
