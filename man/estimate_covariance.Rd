% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariance_estimators.R
\name{estimate_covariance}
\alias{estimate_covariance}
\title{Estimate a Regularised Covariance Matrix}
\usage{
estimate_covariance(
  X,
  y = NULL,
  ytype = c("auto", "continuous", "categorical"),
  method = c("mec", "oas", "sre", "sde", "cse"),
  nslices = 5,
  gamma = 0.1,
  rho = 0.5,
  eps = 1e-08
)
}
\arguments{
\item{X}{Numeric predictor matrix.}

\item{y}{Optional response vector. Required when \code{method = "mec"}.}

\item{ytype}{Character. One of \code{"auto"}, \code{"continuous"}, or \code{"categorical"}.}

\item{method}{Covariance estimator. One of \code{"mec"}, \code{"oas"}, \code{"sre"},
\code{"sde"}, or \code{"cse"}.}

\item{nslices}{Number of slices for MEC with continuous responses.}

\item{gamma}{Ridge constant for SRE.}

\item{rho}{Convex shrinkage intensity for CSE.}

\item{eps}{Positive numerical floor for eigenvalues.}
}
\value{
A list containing the covariance estimate, shrinkage intensity,
and method diagnostics.
}
\description{
Unified interface for covariance estimators used in shrinkage principal
fitted components regression.
}
\examples{
set.seed(1)
X <- matrix(rnorm(120), nrow = 20, ncol = 6)
est <- estimate_covariance(X, method = 'oas')
c(method = est$method, rho = est$rho)
}
