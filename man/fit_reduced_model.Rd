% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/downstream_models.R
\name{fit_reduced_model}
\alias{fit_reduced_model}
\title{Fit Reduced Downstream Model}
\usage{
fit_reduced_model(
  Z,
  y,
  ytype = c("auto", "continuous", "categorical"),
  classifier = c("auto", "glm", "ridge_logistic"),
  standardize = TRUE
)
}
\arguments{
\item{Z}{Numeric reduced predictor matrix.}

\item{y}{Response vector.}

\item{ytype}{Character. One of \code{"auto"}, \code{"continuous"}, or \code{"categorical"}.}

\item{classifier}{Character. Classification model. One of \code{"auto"}, \code{"glm"},
or \code{"ridge_logistic"}.}

\item{standardize}{Logical. If \code{TRUE}, standardise predictors internally in
ridge logistic regression.}
}
\value{
A list containing the fitted downstream model and metadata.
}
\description{
Fits a downstream predictive model using the reduced SPFC score matrix.
}
\examples{
set.seed(1)
Z <- matrix(rnorm(40), nrow = 20, ncol = 2)
y <- Z[, 1] - 0.5 * Z[, 2] + rnorm(20, sd = 0.4)
fit_reduced_model(Z, y, ytype = 'continuous')
}
