% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable_selection.R
\name{spfc_select_variables}
\alias{spfc_select_variables}
\title{Select Variables from SPFC Directions}
\usage{
spfc_select_variables(
  fit,
  method = c("adaptive_weighted_l1", "c1f_extension"),
  selection_rule = c("c1f", "quantile", "fixed"),
  reduced_model = NULL,
  threshold = NULL,
  quantile_cut = 0.75,
  gamma = 1,
  c1f_calibration = c("icomp_hd_floor", "robust_universal", "raw"),
  eps = 1e-10
)
}
\arguments{
\item{fit}{Object returned by \code{spfc_fit()}.}

\item{method}{Character. One of \code{"adaptive_weighted_l1"} or \code{"c1f_extension"}.}

\item{selection_rule}{Character. One of \code{"c1f"}, \code{"quantile"}, or \code{"fixed"}.}

\item{reduced_model}{Fitted reduced model used to obtain the model-parameter covariance when \code{selection_rule = "c1f"}.}

\item{threshold}{Numeric threshold applied to variable importance scores.
If \code{NULL}, a quantile-based threshold is used.}

\item{quantile_cut}{Numeric quantile used when \code{threshold = NULL}.}

\item{gamma}{Positive adaptivity exponent for \code{adaptive_weighted_l1}.}

\item{c1f_calibration}{Character. \code{"icomp_hd_floor"} is the frozen thesis rule that combines a robust empirical loading scale, the high-dimensional multiplicity floor \verb{sqrt(2 log(p) / n)}, and the bounded C1F multiplier \code{1 + C1F/(1 + C1F)}. \code{"robust_universal"} retains the version 0.0.0.9004 calibration for reproducibility, and \code{"raw"} retains the historical uncalibrated C1F route.}

\item{eps}{Small positive numerical constant.}
}
\value{
A data frame containing variable indices, importance scores, weights,
and selected status.
}
\description{
Selects variables using either the thesis-safe adaptive weighted L1 route or
the experimental C1F-informed extension route.
}
\examples{
set.seed(1)
X <- matrix(rnorm(180), nrow = 30, ncol = 6)
y <- X[, 1] - 0.7 * X[, 2] + rnorm(30, sd = 0.5)
fit <- spfc_fit(X, y, d = 1, ytype = 'continuous',
                cov_method = 'oas', poly_degree = 2)
reduced_model <- fit_reduced_model(
  fit$Z, y, ytype = 'continuous'
)
sel <- spfc_select_variables(
  fit, method = 'c1f_extension', selection_rule = 'c1f',
  reduced_model = reduced_model
)
sel[, c('variable', 'shrunk_importance', 'selected')]
}
