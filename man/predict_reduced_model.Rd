% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation_metrics.R
\name{predict_reduced_model}
\alias{predict_reduced_model}
\title{Predict from a Reduced Model Fit}
\usage{
predict_reduced_model(
  object,
  Z,
  type = c("response", "class", "link"),
  threshold = 0.5
)
}
\arguments{
\item{object}{Object returned by \code{fit_reduced_model()}.}

\item{Z}{Numeric reduced-score matrix.}

\item{type}{Character. One of \code{"response"}, \code{"class"}, or \code{"link"}.}

\item{threshold}{Classification threshold for binary probabilities.}
}
\value{
Numeric predictions or factor class labels.
}
\description{
Generates fitted values, probabilities, or predicted classes from an object
returned by \code{fit_reduced_model()}.
}
\examples{
set.seed(1)
Z <- matrix(rnorm(40), nrow = 20, ncol = 2)
y <- Z[, 1] - 0.5 * Z[, 2] + rnorm(20, sd = 0.4)
mod <- fit_reduced_model(Z, y, ytype = 'continuous')
head(predict_reduced_model(mod, Z, type = 'response'))
}
