% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spfc.R
\name{spfc}
\alias{spfc}
\title{Fit a Complete SPFC Workflow}
\usage{
spfc(
  X,
  y,
  d = NULL,
  d_grid = 1:3,
  ytype = c("auto", "continuous", "categorical"),
  cov_method = c("mec", "oas", "sre", "sde", "cse"),
  selector = c("AIC", "BIC", "CAIC", "ICOMP_IFIM", "ICOMP_MISSPEC", "CICOMP"),
  nslices = 5,
  poly_degree = 1,
  variable_method = c("adaptive_weighted_l1", "c1f_extension"),
  selection_rule = c("c1f", "quantile", "fixed"),
  threshold = NULL,
  quantile_cut = 0.75,
  validation = c("resubstitution", "holdout", "cv"),
  test_fraction = 0.3,
  folds = 5,
  validation_seed = 123,
  centre_x = TRUE,
  scale_x = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{X}{Numeric predictor matrix.}

\item{y}{Response vector.}

\item{d}{Optional structural dimension. If \code{NULL}, dimension selection is performed.}

\item{d_grid}{Candidate structural dimensions used when \code{d = NULL}.}

\item{ytype}{Character. One of \code{"auto"}, \code{"continuous"}, or \code{"categorical"}.}

\item{cov_method}{Covariance estimator. One of \code{"mec"}, \code{"oas"}, \code{"sre"}, \code{"sde"}, or \code{"cse"}.}

\item{selector}{Criterion for selecting structural dimension when \code{d = NULL}.}

\item{nslices}{Number of slices for continuous responses.}

\item{poly_degree}{Polynomial degree for continuous response basis.}

\item{variable_method}{Variable selection method.}

\item{selection_rule}{Character screening rule: \code{"c1f"}, \code{"quantile"}, or \code{"fixed"}.}

\item{threshold}{Optional finite numeric screening threshold. It is required for \code{selection_rule = "fixed"}; for \code{"quantile"}, \code{NULL} uses \code{quantile_cut}; it is ignored by the C1F-calibrated rule.}

\item{quantile_cut}{Quantile threshold for variable selection.}

\item{validation}{Character. Validation strategy. One of \code{"resubstitution"},
\code{"holdout"}, or \code{"cv"}.}

\item{test_fraction}{Numeric value between 0 and 1 giving the test-set
proportion when \code{validation = "holdout"}.}

\item{folds}{Integer number of folds to use when \code{validation = "cv"}.}

\item{validation_seed}{Integer seed used for train-test splitting or
cross-validation fold creation.}

\item{centre_x}{Logical. If \code{TRUE}, centre predictor columns.}

\item{scale_x}{Logical. If \code{TRUE}, scale predictor columns.}

\item{verbose}{Logical. If \code{TRUE}, print progress messages.}

\item{...}{Additional arguments passed to \code{spfc_fit()}.}
}
\value{
A list of class \code{spfc} containing the complete workflow.
}
\description{
Fits a complete Shrinkage Principal Fitted Components workflow, including
optional structural dimension selection, SPFC fitting, reduced model fitting,
evaluation, and variable selection.
}
\examples{
\donttest{
set.seed(1)
X <- matrix(rnorm(180), nrow = 30, ncol = 6)
y <- X[, 1] - 0.7 * X[, 2] + rnorm(30, sd = 0.5)
obj <- spfc(
  X, y, d = 1, ytype = 'continuous', cov_method = 'oas',
  poly_degree = 2, selection_rule = 'quantile',
  quantile_cut = 0.75, validation = 'resubstitution',
  verbose = FALSE
)
obj
}
}
